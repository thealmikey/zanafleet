#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🎨 Formatting & linting staged files..."

npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Commit blocked — fix errors above"
  exit 1
fi

echo "✅ Code formatted & clean — committing"
