---
name: lint
description: Use this skill whenever the user asks to lint code, fix lint errors, clean up style issues, or run oxlint. This skill runs oxlint with auto-fix enabled.
---

# Lint Skill (Oxlint)

Always run:

```bash
npx -y oxlint@latest --format unix --fix