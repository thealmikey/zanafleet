/**
 * Role Scope Enum
 * Defines the scope levels at which roles can be applied in ZanaFleet
 */
export enum RoleScope {
  Organization = 'Organization',
  Workspace = 'Workspace',
  Actor = 'Actor',
}
