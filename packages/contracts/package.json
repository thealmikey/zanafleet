{
  "name": "@zanafleet/contracts",
  "version": "0.0.1",
  "description": "Shared DTOs, event interfaces, and type definitions for ZanaFleet",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.1.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
